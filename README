cd ~
git clone https://github.com/vishalShekhar/vimconfig.git .vim
ln -s .vim/vimrc .vimrc
cd .vim
git submodule update --init --recursive
# if need to update the submodules to the latest code 
#./update.sh 


# Adding a new sub module
# cd ~/.vim
# git submodule add -b master https://github.com/davidhalter/jedi-vim.git bundle/jedi
# git add .
# git commit -m "Install Jedi.vim bundle as a submodule."
# git submodule update --init --recursive

# Helpful url : http://vimcasts.org/episodes/synchronizing-plugins-with-git-submodules-and-pathogen/

# Deleting a sub module
# git submodule deinit -f bundle/vim-colors-solarized
# rm -fr .git/modules/bundle/vim-colors-solarized
# git rm -f bundle/vim-colors-solarized
